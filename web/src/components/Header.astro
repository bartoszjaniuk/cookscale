---
import type { User } from "@supabase/supabase-js";
import { NavHeader } from "@/components/nav-header";

interface Props {
  currentPath: string;
  user?: User | null;
}

const { currentPath, user = null } = Astro.props;
---

<NavHeader client:load currentPath={currentPath} isLoggedIn={!!user} />
